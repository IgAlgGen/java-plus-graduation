spring:
  application:
    name: main-service
  cloud:
    config:
      enabled: false
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:stats"
    username: stats
    password: stats

server:
  port: 0

eureka:
  client:
    enabled: false
